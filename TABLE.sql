CREATE TABLE TRAFFIC(
days INT,
hours INT,	
vehicle_count	INT,
average_speed	DECIMAL(6,2),
congestion_level	VARCHAR(10),
weather_condition	VARCHAR(20),
traffic_rule_violation VARCHAR(50)
);

